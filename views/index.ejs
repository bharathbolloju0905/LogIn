<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 flex items-center justify-center h-screen">
  <div class="bg-gray-800 p-8 rounded-lg shadow-lg w-96">
    <h2 class="text-2xl font-bold text-white mb-6 text-center">Login</h2>
    <% if(error.length>0){%>
       <p class="text-red-400"> <%=error%></p>
      <% } %>
    <form  action="/login" method="post">
      <div class="mb-4">
        <label class="block text-gray-400 mb-2" for="email">username
        </label>
        <input type="text" id="email" name="username" class="w-full p-3 rounded bg-gray-700 text-white" placeholder="Enter your email" required>
      </div>
      <div class="mb-4">
        <label class="block text-gray-400 mb-2" for="password">Password</label>
        <input type="password" id="password" name="password" class="w-full p-3 rounded bg-gray-700 text-white" placeholder="Enter your password" required>
      </div>
      <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 rounded-lg">Login</button>
    </form>
    <p class="text-gray-400 mt-6 text-center">Don't have an account? <a href="/signup" class="text-indigo-500">Sign up here</a></p>
  </div>
</body>
</html>
